# Browserbase.CLI

Command-line interface for the Browserbase SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Browserbase.CLI --prerelease
```

## Usage

```bash
browserbase --help
browserbase api --help
```